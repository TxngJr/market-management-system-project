import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

type Props = {}

const CheckListStores = (props: Props) => {
  return (
    <View>
      <Text>CheckListStores</Text>
    </View>
  )
}

export default CheckListStores

const styles = StyleSheet.create({})